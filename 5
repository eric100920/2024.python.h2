
def find_max(a, b):
    if a > b:
        return a
    else:
        return b
def main():
    num1 = float(input("1: "))
    num2 = float(input("2: "))
    max_value = find_max(num1, num2)

    print("1:", max_value)
main()
